{% set version = "0.3.0.75.a7b1eef4" %}
{% set commit = "a7b1eef4b69dc93cef9bc2a2317e2356bbbc3dbe" %}

package:
  name: python-symengine
  version: {{ version }}

source:
  fn: {{ commit }}.tar.gz
  url: https://github.com/symengine/symengine.py/archive/{{ commit }}.tar.gz

build:
  number: 0
  skip: true        # [win and py<35]
  features:
    - vc14          # [win and py>=35]

requirements:
  build:
    - toolchain
    - cmake
    - symengine     0.3.0.237.g398a3f3
    - python
    - cython
    - setuptools
    - llvmdev       >=3.9
  run:
    - symengine     0.3.0.237.g398a3f3
    - python
    - numpy

test:
  requires:
    - pytest
    - sympy
  imports:
    - symengine
  commands:
    - python -c $'import symengine;\nif not symengine.test():\n raise Exception("Tests failed")'    # [unix]

about:
  home: https://github.com/symengine/symengine.py
  license: MIT
  license_file: LICENSE
  summary: 'Python wrappers for SymEngine, a fast symbolic manipulation library, written in C++'

extra:
  recipe-maintainers:
    - isuruf
    - certik
